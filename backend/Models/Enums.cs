namespace ExpenseTracker.API.Models;

public enum Category
{
    Food,
    Transport,
    Housing,
    Health,
    Entertainment,
    Other
}

public enum PaymentMethod
{
    Cash,
    CreditCard,
    DebitCard,
    BankTransfer,
    EWallet
}
